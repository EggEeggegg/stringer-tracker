version: "3"

tasks:
    dev:
        desc: Run dev server with hot reload (air)
        cmds:
            - air

    run:
        desc: Run server (no hot reload)
        cmds:
            - go run ./cmd/server

    build:
        desc: Build binary to bin/server
        cmds:
            - go build -o bin/server ./cmd/server

    migrate:
        desc: Run all SQL migrations in order
        cmds:
            - go run ./cmd/migrate

    seed:
        desc: Reset DB, create admin + user1, seed sample records for user1
        cmds:
            - go run ./cmd/seed

    tidy:
        desc: Tidy Go modules
        cmds:
            - go mod tidy
